# Propose wander/random move operator

sp {propose*wander
    (state <s> ^name smartagent
               ^io.input-link.landmarks.status <landmarks-status>
               {<landmarks-status> <> inactive }) 
-->
    (<s> ^operator.name wander)}